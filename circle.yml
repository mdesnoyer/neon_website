machine:
  post:
    - sudo pip install awscli

deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy.sh production
  staging:
    branch: develop
    commands:
      - ./bin/deploy.sh staging